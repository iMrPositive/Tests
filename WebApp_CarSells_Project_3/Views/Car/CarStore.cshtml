@model List <WebApp_CarSells_Project_3.Models.Table>

@{
    ViewBag.Title = "CarStore";
}


<link href="@Url.Content("~/Stayles/NavPar.css")" rel="stylesheet" type="text/css">
<link href="@Url.Content("~/Stayles/Tap.css")" rel="stylesheet" type="text/css">


<ul>
    <li><a class="active" href="#home">Home</a></li>
    <li >@Html.ActionLink("Main Page", "Index")</li>
    <li class ="active">@Html.ActionLink("Car Store", "CarStore")</li>
    <li>@Html.ActionLink("Add a New Car", "AddCar"))</li>
    <li style="float:right">@Html.ActionLink("Enable Edit", "CarStore")</li>
</ul>

<h2>CarStore</h2>

<table class="table table-hover" style="width:100%">
    @*<tr>
            <th>Car Name</th>
            <th>Owner</th>
            <th>Phone Number</th>
            <th>Price</th>
            <th>Payment_Method</th>
            <th>Transmission</th>
            <th>Condition</th>
            <th>Licensed</th>
            <th></th>
        </tr>*@


    @foreach (var item in Model)
    {
        <tr>
            <th><img src="@Url.Content(item.CarImgPath)" width="200" height="200" /></th>
            <th>Car Name: <br /> @item.Car_Name</th>

            <td>Price: <br /><ava>@item.Price $<ava></td>

            <td>
                @Html.ActionLink("Delete", "Delete", new { id = item.Id }) |
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id })
        </td>
    </tr>
    }
</table>

